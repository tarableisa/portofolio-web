[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn backend.wsgi"
healthcheckPath = "/api/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
